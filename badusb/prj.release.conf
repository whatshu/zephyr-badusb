#
# Release build:
# - Keep USB device stack + HID enabled (from prj.conf)
# - Do not expose CDC/MSC by default
#

# Explicitly disable classes to keep composite device minimal
CONFIG_USBD_CDC_ACM_CLASS=n
CONFIG_USBD_MSC_CLASS=n


